"""PCB Flow — an AI-assisted PCB design harness.

The package ties the 12-phase workflow, the reliability layer, and the automation
tools into one CLI (`pcbflow`). Pure Python 3 standard library at the core; the only
runtime dependency (`websockets`) is used solely by the headless EasyEDA driver.
"""
__version__ = "0.1.0"
