---
name: Bug report
about: Something in the workspace, tooling, or docs didn't work as described
title: "[bug] "
labels: bug
---

**What happened**
A clear description of the bug.

**What you expected**
What should have happened instead.

**To reproduce**
1. Phase / command / file involved:
2. Steps:
3. Board (if a `projects/` example):

**Environment**
- OS: (Linux / macOS / Windows)
- Python version (`python3 --version`):
- AI assistant (Claude Code / Codex / Cursor / …):
- EDA tool + version (EasyEDA Pro / KiCad):
- Output of `python3 tools/doctor.py` (if relevant):

**Logs / output**
Paste relevant output. Scrub any secrets first (`.logs/` is gitignored for a reason).
